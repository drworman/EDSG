"""Tkinter user interfaces for the organizer and participant builds.

Tkinter is used rather than a heavier toolkit because it ships with
CPython, adds no runtime licensing questions, and produces the smallest
single-file binaries of the realistic options. Every widget here is
plain ``tkinter.ttk`` so the result looks native on Windows, macOS and
Linux without shipping a theme engine.
"""

from __future__ import annotations

__all__: list[str] = []
